services:
  creativeimpressionfrontend:
    image: creativeimpressionfrontend
    build:
      context: creative-impression/frontend
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
